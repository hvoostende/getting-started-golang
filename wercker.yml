box:
  id: golang
  ports:
    - "5000"

dev:
  steps:
    - internal/watch:
        code: |
          go build ./...
          ./source
        reload: true
# Build definition
build:
  # The steps that will be executed on build
  steps:

    # golint step!
    - wercker/golint

    # Build the project
    - script:
        name: go build
        code: |
          go build ./...

    # Test the project
    - script:
        name: go test
        code: |
          go test ./...

hantest:
  steps:
    - script:
        name: say something
        code: |
          echo "Han Rules"

woutertest:
  steps:
    - script:
        name: say something
        code: |
          echo "Wouter Rules"

paultest:
  steps:
    - script:
        name: say something
        code: |
          echo "Wouter Rules"

finaltest:
  steps:
    - script:
        name: done
        code: |
          echo "This should allways run last"  
